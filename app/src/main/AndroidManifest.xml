<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gym">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <activity
            android:name=".activites.MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".InteractiveTrainingActivity" />
        <activity android:name=".QRActivity" />
        <activity android:name=".activites.availableGymTicket.AvailableGymTickets" />
        <activity android:name=".activites.MyGymTickets.MyGymTickets" />
        <activity android:name=".activites.availableDietsList.AvailableDietsList" />
        <activity android:name=".MyDimensionChartActivity" />
        <activity android:name=".activites.classesPlan.ClassesDetailsActivity" />
        <activity android:name=".activites.classesPlan.ClassesPlanActivity" />
        <activity android:name=".Niepotrzebne.ChooseWeekActivity" />
        <activity android:name=".activites.myClassesList.MyClassesListActivity" />
        <activity android:name=".activites.availableTreningPlansList.AvailableTrainingPlansListActivity" />
        <activity android:name=".activites.trainingPlansList.MyTrainingPlansListActivity" />
        <activity android:name=".activites.myDietsList.dietsDay.DietsDayActivity" />
        <activity android:name=".activites.myDietsList.DietDaysListActivity" />
        <activity android:name=".Niepotrzebne.Diet_activity" />
        <activity android:name=".activites.myDietsList.MyDietsListActivity" />
        <activity android:name=".activites.gymsList.GymDetailsActivity" />
        <activity android:name=".activites.gymsList.GymsListActivity" />
        <activity android:name=".activites.GymHomePageActivity" />
        <activity android:name=".activites.dieticianList.DieticianListActivity" />
        <activity android:name=".activites.DieticianDetailActivity" />
        <activity android:name=".activites.DieticianHomePageActivity" />
        <activity android:name=".activites.TrainerHomePageActivity" />
        <activity android:name=".activites.workoutsHistory.WorkoutsHistoryActivity" />
        <activity android:name=".activites.dimensionHistory.DimensionHistoryActivity" />
        <activity android:name=".activites.MyStatisticGeneralPageActivity" />
        <activity android:name=".activites.AddDimensionsActivity" />
        <activity android:name=".activites.PasswordChangeActivity" />
        <activity android:name=".activites.MyPersonalDataActivity" />
        <activity android:name=".activites.MyProfileActivity" />
        <activity android:name=".activites.TrainerDetailActivity" />
        <activity android:name=".activites.trainersList.TrainersListActivity" />
        <activity android:name=".activites.HomePageActivity" />
        <activity
            android:name=".activites.RegistrationActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activites.MainActivity"
            android:windowSoftInputMode="stateVisible">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>